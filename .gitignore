supabase/
*.sql.tmp
# Scraper exports are CI artifacts pushed straight to Supabase —
# the client fetches from there, never from the repo. Keep cultural
# agent exports (consumed by sync-agents) but ignore grants radar.
scripts/output/grants_radar_*
scripts/output/grants_export_*
# Nuxt
.nuxt/
.output/
dist/

# Node
node_modules/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Env
.env
.env.*
!.env.example

# Test
playwright-report/
test-results/
test-results-*

# Lock (keep one)
package-lock.json

# Large data files (use build scripts to regenerate)
public/data/species/icmbio-brazil.json
public/data/species/iucn.json

# opencode local config (contains provider API keys)
.opencode.local.json
opencode.local.json
